# Copy to .env for local (non-docker) development.
PORT=3003

# Path to the serialized model, relative to the working directory the
# app is started from. In Docker this is overridden by docker-compose
# to point at the mounted ml-models volume.
MODEL_PATH=../ml-models/moto_cost_model.pkl
